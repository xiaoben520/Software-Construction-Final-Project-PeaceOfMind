<UserControl x:Class="MemoMind.App.Views.FileWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="文件工作区" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" />
                <TextBlock Text="把常用文件或文件夹保存起来，后续一键打开。" Margin="0,8,0,8" Foreground="{DynamicResource SecondaryTextBrush}" />
                <TextBlock Text="{Binding StatusMessage}" Margin="0,0,0,14" Foreground="{DynamicResource SecondaryTextBrush}" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Margin="0,0,12,0" Padding="10,6" Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Grid.Column="1" Margin="0,0,12,0" Padding="10,6" Text="{Binding WorkspacePath, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2" Content="添加" Padding="18,8" Style="{StaticResource SidebarButtonStyle}" Command="{Binding AddWorkspaceCommand}" />
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ListView ItemsSource="{Binding Workspaces}" SelectedItem="{Binding SelectedWorkspace}" BorderThickness="0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" Margin="0,0,0,10" Background="{DynamicResource ListItemBrush}" CornerRadius="10">
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="15" />
                                    <TextBlock Text="{Binding Path}" Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{DynamicResource SecondaryTextBrush}" />
                                    <TextBlock Margin="0,6,0,0" Foreground="{DynamicResource SecondaryTextBrush}">
                                        <Run Text="最近打开：" />
                                        <Run Text="{Binding LastOpenedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" />
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="刷新" Margin="0,0,10,0" Padding="16,8" Style="{StaticResource SidebarButtonStyle}" Command="{Binding RefreshCommand}" />
                    <Button Content="打开" Margin="0,0,10,0" Padding="16,8" Style="{StaticResource SidebarButtonStyle}" Command="{Binding OpenWorkspaceCommand}" />
                    <Button Content="删除" Padding="16,8" Style="{StaticResource SidebarButtonStyle}" Command="{Binding RemoveWorkspaceCommand}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
