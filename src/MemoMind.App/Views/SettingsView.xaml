<UserControl x:Class="MemoMind.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="设置" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" />
                <TextBlock Text="{Binding StatusMessage}" Margin="0,8,0,16" Foreground="{DynamicResource SecondaryTextBrush}" />

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="API Key" />
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" Padding="10,6" Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="主题" />
                    <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" SelectedItem="{Binding Theme}">
                        <sys:String>Light</sys:String>
                        <sys:String>Dark</sys:String>
                    </ComboBox>

                    <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="提醒小时" />
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" Padding="10,6" Text="{Binding ReminderHour, UpdateSourceTrigger=PropertyChanged}" />

                    <CheckBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" Content="启用 AI" IsChecked="{Binding EnableAi}" />
                    <CheckBox Grid.Row="4" Grid.Column="1" Content="启用每日提醒" IsChecked="{Binding EnableReminder}" />
                </Grid>

                <TextBlock Text="左侧栏目显示" Margin="0,20,0,10" FontSize="18" FontWeight="SemiBold" />
                <ItemsControl ItemsSource="{Binding SidebarOptions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,8">
                                <CheckBox Content="{Binding Title}"
                                          IsChecked="{Binding IsSelected}">
                                    <CheckBox.Style>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="IsEnabled" Value="True" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLocked}" Value="True">
                                                    <Setter Property="IsEnabled" Value="False" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </CheckBox.Style>
                                </CheckBox>
                                <TextBlock Text="{Binding Description}" Margin="24,2,0,0" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="主页模块显示" Margin="0,20,0,10" FontSize="18" FontWeight="SemiBold" />
                <ItemsControl ItemsSource="{Binding HomeOptions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,8">
                                <CheckBox Content="{Binding Title}" IsChecked="{Binding IsSelected}" />
                                <TextBlock Text="{Binding Description}" Margin="24,2,0,0" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="重新加载" Margin="0,0,10,0" Padding="16,8" Style="{StaticResource SidebarButtonStyle}" Command="{Binding LoadCommand}" />
                    <Button Content="保存设置" Padding="16,8" Style="{StaticResource SidebarButtonStyle}" Command="{Binding SaveCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </ScrollViewer>
</UserControl>
